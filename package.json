{
  "name": "minimal-lambda",
  "version": "0.0.7",
  "description": "MinimalLambda - Modern .NET framework for building AWS Lambda functions",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
