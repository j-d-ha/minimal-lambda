{
  "name": "minimal-lambda",
  "version": "0.0.7",
  "description": "MinimalLambda - ASP.NET Core Minimal API-style patterns for any AWS Lambda trigger",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
